import { describe,expect,it } from "vitest";import { classifyLinkResponse,isPlatformIncident,nextMediaHealth } from "@/server/domain/link-health";import { assertPublicHttpsUrl } from "@/server/security/safe-url";
describe("link health",()=>{it("ไม่ถือ 403 เป็น broken",()=>expect(classifyLinkResponse({status:403})).toBe("warning"));it("404 เป็น broken และ timeout เป็น unknown",()=>{expect(classifyLinkResponse({status:404})).toBe("broken");expect(classifyLinkResponse({timedOut:true})).toBe("unknown")});it("fail ต่อเนื่องทำ degraded แล้ว suspend",()=>{expect(nextMediaHealth(3).status).toBe("degraded");expect(nextMediaHealth(5).status).toBe("suspended")});it("ตรวจ incident ระดับ platform",()=>{expect(isPlatformIncident(10,7)).toBe(true);expect(isPlatformIncident(4,4)).toBe(false)});it("ป้องกัน SSRF ไป private network",()=>{expect(()=>assertPublicHttpsUrl("http://localhost/a")).toThrow();expect(()=>assertPublicHttpsUrl("https://192.168.1.2/a")).toThrow();expect(assertPublicHttpsUrl("https://wordwall.net/a").hostname).toBe("wordwall.net")})});
