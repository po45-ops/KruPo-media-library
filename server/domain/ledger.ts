import { assertSatang } from "./money";
export type LedgerKind="sale_creator"|"sale_platform"|"payment_fee"|"refund_creator"|"refund_platform"|"payout"|"adjustment";
export type LedgerStatus="pending"|"on_hold"|"available"|"paid"|"reversed";
export interface LedgerEntry{id:string;creatorId?:string|null;kind:LedgerKind;status:LedgerStatus;amountSatang:number;referenceType:string;referenceId:string;reversesEntryId?:string|null;createdAt:string;}
export function ledgerBalance(entries:LedgerEntry[],statuses:LedgerStatus[]=["available"]){return entries.filter(x=>statuses.includes(x.status)).reduce((sum,x)=>sum+assertSatang(Math.abs(x.amountSatang))*Math.sign(x.amountSatang),0)}
export function buildSaleLedger(input:{orderItemId:string;creatorId:string;creatorShareSatang:number;platformShareSatang:number;holdUntil:string}):LedgerEntry[]{const createdAt=new Date().toISOString();return [{id:crypto.randomUUID(),creatorId:input.creatorId,kind:"sale_creator",status:"on_hold",amountSatang:assertSatang(input.creatorShareSatang),referenceType:"order_item",referenceId:input.orderItemId,createdAt},{id:crypto.randomUUID(),creatorId:null,kind:"sale_platform",status:"available",amountSatang:assertSatang(input.platformShareSatang),referenceType:"order_item",referenceId:input.orderItemId,createdAt}]}
export function reverseLedger(entries:LedgerEntry[],refundId:string):LedgerEntry[]{return entries.filter(x=>["sale_creator","sale_platform"].includes(x.kind)).map(x=>({id:crypto.randomUUID(),creatorId:x.creatorId,kind:x.kind==="sale_creator"?"refund_creator":"refund_platform",status:"available",amountSatang:-assertSatang(x.amountSatang),referenceType:"refund",referenceId:refundId,reversesEntryId:x.id,createdAt:new Date().toISOString()}))}
