import "server-only";import { createAdminSupabaseClient } from "@/server/supabase/admin";
export async function bootstrapOwnerIfEligible(user:{id:string;email?:string|null}){const expected=process.env.BOOTSTRAP_ADMIN_EMAIL?.trim().toLocaleLowerCase();if(!expected||user.email?.trim().toLocaleLowerCase()!==expected)return false;const {data,error}=await createAdminSupabaseClient().rpc("bootstrap_owner_role",{p_user_id:user.id,p_expected_email:expected});if(error)throw new Error("สร้าง owner role และ Audit Log ไม่สำเร็จ");return data===true}
