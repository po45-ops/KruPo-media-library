import type { PaymentProvider } from "./payment-provider";export class DisabledPaymentProvider implements PaymentProvider{readonly name="disabled" as const;async createPayment():Promise<never>{throw new Error("ระบบชำระเงินกำลังเปิดให้บริการ")}async verifyWebhook():Promise<never>{throw new Error("Payment provider ปิดอยู่")}async refund():Promise<never>{throw new Error("Payment provider ปิดอยู่")}async health(){return {status:"warning" as const,message:"ปิดระบบชำระเงินตามการตั้งค่า"}}}
