import type { FileSafetyProvider } from "./file-safety-provider";
export class BasicFileSafetyChecker implements FileSafetyProvider{readonly name="basic-signature";async check(bytes:Uint8Array,fileName:string,mimeType:string){const head=new TextDecoder().decode(bytes.slice(0,2048));const signatures:string[]=[];if(head.includes("<script")||head.includes("javascript:"))signatures.push("active_script_marker");if(/\.(exe|dll|com|scr|bat|cmd|ps1)$/i.test(fileName))signatures.push("executable_extension");if(signatures.length)return {status:"unsafe" as const,provider:this.name,reason:"พบรูปแบบไฟล์ที่ไม่อนุญาต",signatures,checkedAt:new Date().toISOString()};return {status:"manual_review" as const,provider:this.name,reason:`Basic check ผ่าน (${mimeType}) แต่ต้องใช้ malware scanner ก่อนอนุมัติไฟล์ Production`,signatures:[],checkedAt:new Date().toISOString()}}}
