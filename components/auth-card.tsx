import Image from "next/image";
import Link from "next/link";
import { LockKeyhole, Mail, ShieldCheck } from "lucide-react";

export function AuthCard({mode,next}:{mode:"login"|"register"|"forgot";next?:string}){
 const isLogin=mode==="login", isRegister=mode==="register";
 return <div className="grid min-h-[720px] bg-gradient-to-br from-[#E9F4FF] via-[#F8FBFF] to-[#EFEAFF] lg:grid-cols-[1fr_560px_1fr]"><div className="hidden items-end justify-end p-10 lg:flex"><Image src="/brand/hero-learning.svg" alt="อุปกรณ์การเรียนรู้" width={450} height={280}/></div><div className="m-4 self-center rounded-[28px] border border-white/80 bg-white p-7 shadow-2xl shadow-blue-950/10 sm:p-10"><div className="text-center"><Image className="mx-auto" src="/brand/logo.svg" alt="KruPo คลังสื่อ" width={180} height={48}/><h1 className="mt-7 text-2xl font-black text-[#0B2F6B]">{isLogin?"เข้าสู่ระบบ":isRegister?"สมัครสมาชิก":"ลืมรหัสผ่าน"}</h1><p className="mt-2 text-sm text-[#66758A]">{mode==="forgot"?"เราจะส่งลิงก์ตั้งรหัสผ่านใหม่ให้คุณ":"ใช้บัญชีเดียวเพื่อเก็บสื่อที่ได้รับสิทธิ์ไว้ตลอด"}</p></div><form method="post" action={`/api/auth/${mode}`} className="mt-8 grid gap-5">{next&&<input type="hidden" name="next" value={next}/>} {isRegister&&<label className="grid gap-2 text-sm font-bold">ชื่อที่แสดง<input required name="displayName" autoComplete="name" className="field" placeholder="เช่น ครูสมชาย"/></label>}<label className="grid gap-2 text-sm font-bold">อีเมล<div className="relative"><Mail className="absolute left-4 top-3.5 h-5 w-5 text-[#66758A]"/><input required type="email" name="email" autoComplete="email" className="field pl-12" placeholder="name@example.com"/></div></label>{mode!=="forgot"&&<label className="grid gap-2 text-sm font-bold">รหัสผ่าน<div className="relative"><LockKeyhole className="absolute left-4 top-3.5 h-5 w-5 text-[#66758A]"/><input required minLength={10} type="password" name="password" autoComplete={isLogin?"current-password":"new-password"} className="field pl-12" placeholder="อย่างน้อย 10 ตัวอักษร"/></div></label>}{isRegister&&<label className="flex items-start gap-3 text-sm leading-6 text-[#66758A]"><input required type="checkbox" className="mt-1"/>ฉันยอมรับ <Link href="/terms" className="font-bold text-[#0F5BD8]">เงื่อนไขการใช้งาน</Link> และนโยบายความเป็นส่วนตัว</label>}<button className="btn btn-primary w-full" type="submit">{isLogin?"เข้าสู่ระบบ":isRegister?"สร้างบัญชี":"ส่งลิงก์ตั้งรหัสผ่าน"}</button></form>{mode!=="forgot"&&<><div className="my-5 flex items-center gap-3 text-xs text-[#66758A]"><span className="h-px flex-1 bg-[#E1E8F2]"/>หรือ<span className="h-px flex-1 bg-[#E1E8F2]"/></div><a className="btn btn-secondary w-full" href={`/api/auth/google${next?`?next=${encodeURIComponent(next)}`:""}`}>เข้าสู่ระบบด้วย Google</a></>}<p className="mt-6 text-center text-sm text-[#66758A]">{isLogin?<>ยังไม่มีบัญชี? <Link className="font-bold text-[#0F5BD8]" href="/register">สมัครสมาชิก</Link></>:isRegister?<>มีบัญชีแล้ว? <Link className="font-bold text-[#0F5BD8]" href="/login">เข้าสู่ระบบ</Link></>:<Link className="font-bold text-[#0F5BD8]" href="/login">กลับไปเข้าสู่ระบบ</Link>}</p><p className="mt-6 flex items-center justify-center gap-2 text-xs text-[#66758A]"><ShieldCheck className="h-4 w-4 text-[#2FC58D]"/>ระบบจะไม่ส่งรหัสผ่านไปยังผู้ดูแล</p></div><div className="hidden items-start p-10 lg:flex"><div className="mt-24 rounded-3xl bg-white/70 p-6 text-[#0B2F6B]"><p className="text-4xl">✓</p><p className="mt-3 font-extrabold">ซื้อครั้งเดียว ใช้ผ่านบัญชีเดิม</p><p className="mt-2 text-sm text-[#66758A]">สิทธิ์ผูกกับรหัสผู้ใช้ ไม่หายเมื่อเปลี่ยนอีเมล</p></div></div></div>
}
