import { MediaCard } from "@/components/media-card";
import { listPublishedMedia } from "@/server/repositories/catalog-repository";
export default async function GamesPage(){const games=await listPublishedMedia({mediaType:"เกมการศึกษา",limit:60});return <div className="container-page py-10"><div className="rounded-[28px] bg-gradient-to-r from-[#0B2F6B] to-[#6554E8] p-8 text-white"><p className="font-bold text-[#BEEFE0]">Game Link First</p><h1 className="mt-2 text-4xl font-black">เกมการศึกษาที่พร้อมใช้</h1><p className="mt-3 max-w-2xl text-white/80">คัดเลือกเกมสำหรับห้องเรียน พร้อมตรวจสุขภาพลิงก์และควบคุมสิทธิ์ฝั่งเซิร์ฟเวอร์</p></div>{games.length?<div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{games.map(m=><MediaCard key={m.id} media={m}/>)}</div>:<div className="soft-card mt-8 p-10 text-center text-[#66758A]">ยังไม่มีเกมที่ผ่านการอนุมัติ</div>}</div>}
