import { NextResponse } from "next/server";import { getPaymentProvider } from "@/providers/payments";import { createAdminSupabaseClient } from "@/server/supabase/admin";
export const dynamic="force-dynamic";export async function POST(request:Request){const payload=await request.text(),signature=request.headers.get("stripe-signature");try{const provider=getPaymentProvider();const event=await provider.verifyWebhook(payload,signature);const db=createAdminSupabaseClient();const payloadHash=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(payload)).then(x=>[...new Uint8Array(x)].map(b=>b.toString(16).padStart(2,"0")).join(""));const {data,error}=event.kind==="refund"?await db.rpc("process_verified_refund_event",{p_provider:provider.name,p_event_id:event.eventId,p_provider_refund_id:event.providerRefundId,p_provider_payment_id:event.providerPaymentId??null,p_status:event.status,p_amount_satang:event.amountSatang,p_event_type:event.rawType,p_payload_hash:payloadHash,p_failure_reason:event.failureReason??null}):await db.rpc("process_verified_payment_event",{p_provider:provider.name,p_event_id:event.eventId,p_provider_payment_id:event.providerPaymentId,p_order_id:event.orderId,p_status:event.status,p_amount_satang:event.amountSatang,p_event_type:event.rawType,p_payload_hash:payloadHash});if(error)throw error;return NextResponse.json({received:true,result:data})}catch(error){const record=error&&typeof error==="object"?error as Record<string,unknown>:{};console.error("payment_webhook_failed",{name:typeof record.name==="string"?record.name:"Error",code:typeof record.code==="string"?record.code:undefined,message:error instanceof Error?error.message:"unknown"});return NextResponse.json({error:"Webhook verification failed"},{status:400})}}
