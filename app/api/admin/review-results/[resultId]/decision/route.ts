import {NextResponse} from "next/server";import {z} from "zod";import {getRequestPrincipal,hasRole} from "@/server/auth/principal";import {createAdminSupabaseClient} from "@/server/supabase/admin";import {isSameOriginMutation} from "@/server/security/request-origin";
const schema=z.object({decision:z.enum(["accept","reject","request_more_information"]),reason:z.string().trim().min(8).max(1000)});export async function POST(request:Request,{params}:{params:Promise<{resultId:string}>}){if(!isSameOriginMutation(request))return NextResponse.json({error:"invalid origin"},{status:403});const principal=await getRequestPrincipal();if(!principal)return NextResponse.json({error:"unauthorized"},{status:401});if(!hasRole(principal,["admin","reviewer"]))return NextResponse.json({error:"forbidden"},{status:403});const {resultId}=await params,body=schema.safeParse(await request.json().catch(()=>null));if(!z.string().uuid().safeParse(resultId).success||!body.success)return NextResponse.json({error:"ข้อมูลไม่ถูกต้อง"},{status:400});const {error}=await createAdminSupabaseClient().rpc("record_media_risk_decision",{p_result_id:resultId,p_decision:body.data.decision,p_reason:body.data.reason,p_actor_id:principal.userId});return error?NextResponse.json({error:"บันทึกคำตัดสินไม่สำเร็จ"},{status:409}):NextResponse.json({ok:true})}
