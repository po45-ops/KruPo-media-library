import { NextResponse } from "next/server";import { createServerSupabaseClient,isSupabaseConfigured } from "@/server/supabase/server";import { safeReturnPath } from "@/server/security/return-path";export async function GET(request:Request){const url=new URL(request.url),origin=url.origin;if(!isSupabaseConfigured())return NextResponse.redirect(new URL("/login?error=auth_not_configured",origin));const next=safeReturnPath(url.searchParams.get("next"));const client=await createServerSupabaseClient();const callback=new URL("/auth/callback",origin);callback.searchParams.set("next",next);const {data,error}=await client.auth.signInWithOAuth({provider:"google",options:{redirectTo:callback.toString()}});if(error||!data.url)return NextResponse.redirect(new URL("/login?error=oauth_failed",origin));return NextResponse.redirect(data.url)}
